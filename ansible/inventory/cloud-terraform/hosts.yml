all:
  hosts:
    control-plane:
      ansible_host: TBD
    worker-1:
      ansible_host: TBD
    worker-2:
      ansible_host: TBD

control_plane:
  hosts:
    control-plane:

workers:
  hosts:
    worker-1:
    worker-2:

k8s_cluster:
  children:
    control_plane:
    workers:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ../terrafrom/generated/kubeadm-lab.pem
    ansible_python_interpreter: /usr/bin/python3
